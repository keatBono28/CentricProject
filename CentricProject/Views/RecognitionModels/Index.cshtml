@model IEnumerable<CentricProject.Models.RecognitionModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Recognized.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model..)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.coreValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comments)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.createDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Recognized.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Recognizer.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.coreValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comments)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.createDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.recognitionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.recognitionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.recognitionId })
        </td>
    </tr>
}

</table>
